#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - returns the length of a string.
 *@s:The characteres to be checked.
 *
 * Return: a and b.
 */


int _strlen(char *s)
{
return strlen(s);
}
